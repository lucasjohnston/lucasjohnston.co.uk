// @flow

// Variables
const GUTTER_MARGIN = '2rem'

// Fonts
const withSansSerif = () =>
  `font-family: -apple-system, BlinkMacSystemFont, 'avenir next', avenir, helvetica, 'helvetica neue', ubuntu, roboto, noto, 'segoe ui', arial, sans-serif;`
const withSerif = () => `font-family: georgia, times, serif;`
const withSystemSansSerif = () => `font-family: sans-serif;`
const withSystemSerif = () => `font-family: serif;`
const withCode = () => `font-family: Consolas, monaco, monospace;`
const withCourier = () => `font-family: 'Courier Next', courier, monospace;`
const withHelvetica = () =>
  `font-family: 'helvetica neue', helvetica, sans-serif;`
const withAvenir = () => `font-family: 'avenir next', avenir, sans-serif;`
const withAthelas = () => `font-family: athelas, georgia, serif;`
const withGeorgia = () => `font-family: georgia, serif;`
const withTimes = () => `font-family: times, serif;`
const withBodoni = () => `font-family: "Bodoni MT", serif;`
const withCalisto = () => `font-family: "Calisto MT", serif;`
const withGaramond = () => `font-family: garamond, serif;`
const withBaskerville = () => `font-family: baskerville, serif;`

export {
  GUTTER_MARGIN,
  withSansSerif,
  withSerif,
  withSystemSansSerif,
  withSystemSerif,
  withCode,
  withCourier,
  withHelvetica,
  withAvenir,
  withAthelas,
  withGeorgia,
  withTimes,
  withBodoni,
  withCalisto,
  withGaramond,
  withBaskerville,
}
